using System;
using UnityEngine;

public class Needs : MonoBehaviour, IAnimalComponent {

    private AnimalEvents events;

    public void Setup(Genome genome, AnimalEvents animalEvents) {

    }
}

// TODO: Need to implement how time will pass before we can do needs